# Snippet Selections — 2026-05-08 — C/C++

Issue: #13
Date: 2026-05-08
Language: C/C++

## Repo 1

- repo: hyprwm/Hyprland
- url: https://github.com/hyprwm/Hyprland
- description: independent, highly customizable dynamic tiling Wayland compositor with plugin support and visual effects — v0.49.0 released May 8, 2026 with config overhaul, new sudo plugin security, and fine-grained special workspace warp control
- source: GitHub Trending (C++, weekly)

## Repo 2

- repo: ikawrakow/ik_llama.cpp
- url: https://github.com/ikawrakow/ik_llama.cpp
- description: llama.cpp fork with SOTA quantization types (IQ2_K–IQ5_K), improved CPU/GPU hybrid inference, and fused MoE for DeepSeek — active May 2026 enhancement requests; growing as the high-performance alternative to upstream llama.cpp
- source: GitHub Trending (C++, weekly)

## Snippet Selections

### Repo 1

- file_path: src/managers/animation/AnimationManager.cpp
- snippet_url: https://github.com/hyprwm/Hyprland/blob/main/src/managers/animation/AnimationManager.cpp
- ranking: 1 (of 3)

```cpp
static void updateColorVariable(
    CAnimatedVariable<CHyprColor>& av,
    const float POINTY,
    bool warp = false
) {
    if (warp || av.value() == av.goal()) {
        av.warp(true, false);
        return;
    }

    const auto& L1 = av.begun().asOkLab();
    const auto& L2 = av.goal().asOkLab();

    static const auto lerp = [](
        const float one,
        const float two,
        const float progress
    ) -> float {
        return one + ((two - one) * progress);
    };

    const Hyprgraphics::CColor lerped =
        Hyprgraphics::CColor::SOkLab{
            .l = lerp(L1.l, L2.l, POINTY),
            .a = lerp(L1.a, L2.a, POINTY),
            .b = lerp(L1.b, L2.b, POINTY),
        };

    av.value() = {
        lerped,
        lerp(av.begun().a, av.goal().a, POINTY)
    };
}
```

### Repo 2

- file_path: ggml/src/iqk/iqk_cpu_ops.cpp
- snippet_url: https://github.com/ikawrakow/ik_llama.cpp/blob/main/ggml/src/iqk/iqk_cpu_ops.cpp
- ranking: 2 (of 3)

```cpp
void iqk_sumrows_div(
    struct ggml_tensor * div,
    int ith, int nth) {
    auto src = div->src[0];
    GGML_ASSERT(src->type == GGML_TYPE_F32);
    GGML_ASSERT(div->type == GGML_TYPE_F32);

    int ne00  = src->ne[0];
    int nrows = ggml_nrows(src);
    int npt   = (nrows + nth - 1)/nth;
    int first = ith*npt;
    int last  = std::min(first + npt, nrows);
    if (last < first) return;

    for (int ir = first; ir < last; ++ir) {
        auto values = (const float *)(
            (const char *)src->data
            + ir*src->nb[1]);
        float sum = 0;
        for (int j = 0; j < ne00; ++j)
            sum += values[j];
        float norm = sum > 0 ? 1/sum : 0.0f;
        auto result = (float *)(
            (char *)div->data
            + ir*div->nb[1]);
        for (int j = 0; j < ne00; ++j)
            result[j] = values[j]*norm;
    }
}
```
