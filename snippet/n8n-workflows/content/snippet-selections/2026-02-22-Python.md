# Snippet Selections — 2026-02-22 — Python

Issue: 1
Date: 2026-02-22
Language: Python

## Repo 1

- repo: p-e-w/heretic
- url: https://github.com/p-e-w/heretic
- description: Fully automatic censorship removal for language models — v1.2.0 released last week, hit #1 on GitHub trending Python with nearly 4k stars in a week amid controversy
- source: GitHub Trending Python (this week)

## Repo 2

- repo: freemocap/freemocap
- url: https://github.com/freemocap/freemocap
- description: Free, open source motion capture for everyone — strong community momentum with 1,131 stars this week
- source: GitHub Trending Python (this week)

## Repo 3

- repo: roboflow/trackers
- url: https://github.com/roboflow/trackers
- description: Clean, modular re-implementations of leading multi-object tracking algorithms under Apache 2.0 — new dedicated tracking library from Roboflow
- source: GitHub Trending Python (this week)

## Snippet Selections

### Repo 1

- file_path: src/heretic/evaluator.py
- snippet_url: https://github.com/p-e-w/heretic/blob/master/src/heretic/evaluator.py
- ranking: 2 (of 3)

```python
    def is_refusal(self, response: str) -> bool:
        # Classify empty responses as refusals to avoid optimizing for them.
        if not response.strip():
            return True

        # Remove emphasis (e.g. "I *will not*...").
        response = response.lower().replace("*", "")

        # Normalize typographic apostrophes ("won't" -> "won't").
        response = response.replace("'", "'")

        # Normalize whitespace between words to a single space.
        response = " ".join(response.split())

        for marker in self.settings.refusal_markers:
            if marker.lower() in response:
                return True

        return False
```

### Repo 2

- file_path: freemocap/core_processes/capture_volume_calibration/by_camera_reprojection_filtering.py
- snippet_url: https://github.com/freemocap/freemocap/blob/main/freemocap/core_processes/capture_volume_calibration/by_camera_reprojection_filtering.py
- ranking: 1 (of 3)

```python
def _set_unincluded_data_to_nans(
    input_2d_data: np.ndarray,
    frames_with_reprojection_error: list,
    markers_with_reprojection_error: list,
    cameras_to_remove: list[list[int]],
) -> np.ndarray:
    data_to_reproject = input_2d_data.copy()
    for list_of_cameras, frame, marker in zip(
        cameras_to_remove, frames_with_reprojection_error, markers_with_reprojection_error
    ):
        for camera in list_of_cameras:
            data_to_reproject[camera, frame, marker, :] = np.nan
    return data_to_reproject
```

### Repo 3

- file_path: trackers/core/bytetrack/tracker.py
- snippet_url: https://github.com/roboflow/trackers/blob/develop/trackers/core/bytetrack/tracker.py
- ranking: 1 (of 3)

```python
    def _get_associated_indices(
        self,
        similarity_matrix: np.ndarray,
        min_similarity_thresh: float,
    ) -> tuple[list[tuple[int, int]], set[int], set[int]]:
        matched_indices = []
        n_tracks, n_detections = similarity_matrix.shape
        unmatched_tracks = set(range(n_tracks))
        unmatched_detections = set(range(n_detections))

        if n_tracks > 0 and n_detections > 0:
            row_indices, col_indices = linear_sum_assignment(
                similarity_matrix, maximize=True
            )
            for row, col in zip(row_indices, col_indices):
                if similarity_matrix[row, col] >= min_similarity_thresh:
                    matched_indices.append((row, col))
                    unmatched_tracks.remove(row)
                    unmatched_detections.remove(col)

        return matched_indices, unmatched_tracks, unmatched_detections
```
