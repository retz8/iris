# Snippet Selections — 2026-08-21 — C/C++

Issue: #27
Date: 2026-08-21
Language: C/C++

## Repo 1

- repo: deepseek-ai/3FS
- url: https://github.com/deepseek-ai/3FS
- description: High-performance distributed file system designed for AI training and inference, achieving 7.3 TB/s total read throughput — continuing to trend as the open AI-training filesystem reference implementation
- source: GitHub C++ Trending (weekly)

## Repo 2

- repo: justcallmekoko/ESP32Marauder
- url: https://github.com/justcallmekoko/ESP32Marauder
- description: Suite of WiFi and Bluetooth offensive and defensive security tools for the ESP32 microcontroller — trending on GitHub C++ this week, popular with hardware security researchers and red teamers
- source: GitHub C++ Trending (weekly)

## Snippet Selections

### Repo 1

- file_path: src/common/utils/LruCache.h
- snippet_url: https://github.com/deepseek-ai/3FS/blob/main/src/common/utils/LruCache.h
- ranking: 3 (of 3)

```cpp
size_t evictObsoleted() {
  if (least_.size() <= capacity_) {
    return 0;
  }
  auto evictingSize =
      least_.size() - capacity_;
  for (size_t i = 0; i < evictingSize; ++i) {
    used_.erase(back().first);
    least_.pop_back();
  }
  return evictingSize;
}
```

### Repo 2

- file_path: esp32_marauder/MarauderMacAddress.cpp
- snippet_url: https://github.com/justcallmekoko/ESP32Marauder/blob/master/esp32_marauder/MarauderMacAddress.cpp
- ranking: 2 (of 3)

```cpp
bool formatMacAddress(
    const uint8_t mac[kMacAddressSize],
    char output[kMacAddressTextLength + 1]) {
  if (mac == nullptr || output == nullptr) {
    return false;
  }
  for (size_t index = 0;
       index < kMacAddressSize; ++index) {
    const size_t offset = index * 3;
    output[offset] =
        kHexDigits[(mac[index] >> 4) & 0x0F];
    output[offset + 1] =
        kHexDigits[mac[index] & 0x0F];
    if (index + 1 < kMacAddressSize) {
      output[offset + 2] = ':';
    }
  }
  output[kMacAddressTextLength] = '\0';
  return true;
}
```
