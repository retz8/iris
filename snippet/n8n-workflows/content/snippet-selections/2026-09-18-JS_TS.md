# Snippet Selections — 2026-09-18 — JS/TS

Issue: #31
Date: 2026-09-18
Language: JS/TS

## Repo 1

- repo: Tencent/BrowserSkill
- url: https://github.com/Tencent/BrowserSkill
- description: Lets AI agents borrow a real logged-in browser tab without interrupting the user — announced Sept 16-17 2026 on GitHub and Twitter, v0.3.0, supports Claude Code, Cursor, and Codex
- source: GitHub trending weekly, Tencent AI Twitter announcement Sept 16-17 2026

## Repo 2

- repo: langwatch/langwatch
- url: https://github.com/langwatch/langwatch
- description: Open-source platform for LLM evaluation, tracing, and AI agent testing — v3.0 shipped April 2026 with a new scale-ready trace backend and Helm chart support
- source: GitHub trending weekly, langwatch.ai blog April 2026

## Snippet Selections

### Repo 1

- file_path: apps/extension/src/tools/observation.ts
- snippet_url: https://github.com/Tencent/BrowserSkill/blob/main/apps/extension/src/tools/observation.ts
- ranking: 2 (of 3)

```typescript
function truncateBytes(
  html: string,
  maxBytes: number,
): { out: string; truncated: boolean } {
  const enc = new TextEncoder();
  const bytes = enc.encode(html);
  if (bytes.length <= maxBytes) {
    return { out: html, truncated: false };
  }
  // Walk back to a UTF-8 boundary
  // (bytes whose high bits aren't `10`).
  let end = maxBytes;
  while (
    end > 0 &&
    (bytes[end] & 0b1100_0000) === 0b1000_0000
  ) {
    end -= 1;
  }
  const out = new TextDecoder("utf-8", { fatal: false })
    .decode(bytes.subarray(0, end));
  return { out, truncated: true };
}
```

### Repo 2

- file_path: packages/redaction/src/secrets.ts
- snippet_url: https://github.com/langwatch/langwatch/blob/main/packages/redaction/src/secrets.ts
- ranking: 1 (of 3)

```typescript
function shannonEntropyBits(value: string): number {
  const sample =
    value.length > ENTROPY_SAMPLE_LENGTH
      ? value.slice(0, ENTROPY_SAMPLE_LENGTH)
      : value;
  const counts = new Map<string, number>();
  for (const char of sample) {
    counts.set(char, (counts.get(char) ?? 0) + 1);
  }
  let entropy = 0;
  for (const count of counts.values()) {
    const probability = count / sample.length;
    entropy -= probability * Math.log2(probability);
  }
  return entropy;
}
```
