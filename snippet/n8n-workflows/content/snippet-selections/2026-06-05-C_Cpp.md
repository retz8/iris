# Snippet Selections — 2026-06-05 — C/C++

Issue: #17
Date: 2026-06-05
Language: C/C++

## Repo 1

- repo: amnezia-vpn/amnezia-client
- url: https://github.com/amnezia-vpn/amnezia-client
- description: cross-platform VPN client supporting OpenVPN, WireGuard, AmneziaWG, and XRay Reality, with the ability to disguise VPN traffic as DNS queries, QUIC, or SIP calls to evade deep packet inspection
- source: AmneziaWG 2.0 released in 2026 introducing CPS traffic-disguise system; pushed June 5, 2026, trending on GitHub C++ this week

## Repo 2

- repo: tenstorrent/tt-metal
- url: https://github.com/tenstorrent/tt-metal
- description: low-level C++ kernel programming SDK and operator library for Tenstorrent AI accelerator hardware, giving developers direct access to RISC-V cores, NoC fabric, and matrix/vector engines inside Tensix
- source: Tenstorrent gaining developer mindshare as NVIDIA alternative; tt-metal pushed June 5, 2026 with active DeepSeek and Llama kernel work

## Snippet Selections

### Repo 1

- file_path: client/core/utils/networkUtilities.cpp
- snippet_url: https://github.com/amnezia-vpn/amnezia-client/blob/dev/client/core/utils/networkUtilities.cpp
- ranking: 3 (of 3)

```cpp
bool NetworkUtilities::checkIpSubnetFormat(const QString &ip)
{
    if (!ip.contains("/"))
        return checkIPv4Format(ip);

    QStringList parts = ip.split("/");
    if (parts.size() != 2)
        return false;

    bool ok;
    int subnet = parts.at(1).toInt(&ok);
    if (subnet >= 0 && subnet <= 32 && ok)
        return checkIPv4Format(parts.at(0));
    else
        return false;
}
```

### Repo 2

- file_path: tt_metal/impl/graph/graph_tracking.cpp
- snippet_url: https://github.com/tenstorrent/tt-metal/blob/main/tt_metal/impl/graph/graph_tracking.cpp
- ranking: 3 (of 3)

```cpp
bool GraphTracker::hook_allocate(
    const Buffer* buffer) {
    if (hook == nullptr) {
        return false;
    }

    bool hooked = hook->hook_allocate(buffer);
    if (hooked) {
        std::lock_guard<std::mutex> lock(
            hooked_buffers_mutex);
        bool inserted =
            hooked_buffers.insert(buffer).second;
        TT_FATAL(inserted,
            "Can't hook allocation of a buffer "
            "which is already allocated");
    }
    return hooked;
}

bool GraphTracker::hook_deallocate(
    Buffer* buffer) {
    if (hook == nullptr) {
        return false;
    }

    bool hooked = hook->hook_deallocate(buffer);
    if (hooked) {
        std::lock_guard<std::mutex> lock(
            hooked_buffers_mutex);
        auto buffer_it =
            hooked_buffers.find(buffer);
        if (buffer_it == hooked_buffers.end()) {
            log_warning(tt::LogMetal,
                "Can't hook deallocation of a "
                "buffer which allocation "
                "wasn't hooked");
        } else {
            hooked_buffers.erase(buffer_it);
        }
    }
    return hooked;
}
```
