# Snippet Selections — 2026-08-07 — C/C++

Issue: #25
Date: 2026-08-07
Language: C/C++

## Repo 1

- repo: godotengine/godot
- url: https://github.com/godotengine/godot
- description: Multi-platform 2D and 3D open-source game engine at 4.7.x stable / 4.8 dev cycle; consistently trending with 115k stars and strong community-driven compatibility improvements
- source: GitHub Trending (weekly)

## Repo 2

- repo: microsoft/microsoft-ui-xaml
- url: https://github.com/microsoft/microsoft-ui-xaml
- description: WinUI modern UI framework for building dynamic Windows applications; trending as Windows developers migrate to WinUI 3 for next-gen app development (7.8k stars)
- source: GitHub Trending (weekly)

## Snippet Selections

### Repo 1

- file_path: core/object/ref_counted.cpp
- snippet_url: https://github.com/godotengine/godot/blob/master/core/object/ref_counted.cpp
- ranking: 2 (of 3)

```cpp
bool RefCounted::reference() {
	uint32_t rc_val = refcount.refval();
	bool success = rc_val != 0;

	if (success && rc_val <= 2 /* higher is not relevant */) {
		if (get_script_instance()) {
			get_script_instance()->refcount_incremented();
		}
		if (_get_extension() && _get_extension()->reference) {
			_get_extension()->reference(_get_extension_instance());
		}

		_instance_binding_reference(true);
	}

	return success;
}
```

### Repo 2

- file_path: dxaml/xcp/core/sw/utils.cpp
- snippet_url: https://github.com/microsoft/microsoft-ui-xaml/blob/main/dxaml/xcp/core/sw/utils.cpp
- ranking: 2 (of 3)

```cpp
XFLOAT
GetBezierDistance(
    _In_ XFLOAT eDot,
    _In_ XFLOAT eRadius
)
{
    XFLOAT eSquared = eRadius * eRadius;

    XFLOAT eDist = 0;

    XFLOAT eCos = (eSquared + eDot) / 2.0f;
    XFLOAT eSin = 0.0f;

    if (eCos < 0.0f)
        goto Cleanup;

    eSin = eSquared - eCos;

    if (eSin <= 0.0f)
        goto Cleanup;

    eSin = sqrtf(eSin);
    eCos = sqrtf(eCos);

    eDist = FOUR_THIRDS * (eRadius - eCos);

    if (eDist <= eSin * FUZZ)
    {
        eDist = 0;
    }
    else
    {
        eDist = FOUR_THIRDS * (eRadius - eCos) / eSin;
    }

Cleanup:
    return eDist;
}
```
